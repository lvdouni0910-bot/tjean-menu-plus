# TJean Menu+ 运营操作

## 后台入口

正式站点：https://tjean-menu-plus.netlify.app/admin/

后台密钥由 Netlify 项目环境变量 `TJEAN_ADMIN_KEY` 设置。前往 https://app.netlify.com/projects/tjean-menu-plus 的 Environment variables 查看或轮换；不要将密钥写入 GitHub。后台页面可打开，但读取和修改数据必须提供正确密钥。

## 新增菜谱

1. 打开后台，输入密钥，在“菜谱”页填写菜名、唯一英文 Slug、人数、分类及食材和步骤。
2. “机型参数 JSON”的键必须与产品机型名称完全一致：`AirChef X1`、`ShellBake S1`、`Dolphin S1`、`LumiBake S1 Pro`、`UBaker S1`、`ShellBake S2`、`JoyBake`。每个机型的参数包含 `mode`、`temp`、`time`、`rack`。
3. 可在“菜谱图片”处选择一张 JPG、PNG 或 WebP 封面图（不超过 4 MB）。保存菜谱时自动上传；编辑已有菜谱时可替换或删除。未上传时前台显示 Emoji。图片保存在 Netlify Blobs，菜谱数据仅保存引用。\n4. 先保存为“草稿”，核对食材、步骤和机型参数后再切换“已发布”。勾选“免费”的菜谱无需激活即可查看；“机主专属”需要激活。
5. 设置“更新月份”后，已发布菜谱会出现在对应的月度更新页面。

注意：现有代码的菜谱多语言数据仅内置了最初四道示例菜。后台新增的菜谱目前只有录入的原文，切换语言不会自动生成食材和步骤翻译；如面向多语言用户发布，应先补充翻译支持。

## 订单授权

1. 在“订单授权”页选择 TikTok 或 Shopee，填入平台订单号和对应烤箱型号，单条添加或批量导入。批量每行格式为 `平台,订单号,机型`。
2. 客户打开 https://tjean-menu-plus.netlify.app/activate/ ，选择购买平台，填写邮箱或手机号和订单号。验证通过后，设备上保存激活令牌。
3. “订单授权列表”可以查看是否已激活；尚未激活的订单可以删除，已激活订单不能通过当前后台删除。

## 当前权限范围

- 免费菜谱：所有访客可查看。
- 机主专属菜谱：拥有有效激活令牌的用户可查看食材与步骤。烤箱参数只返回激活机型对应的数据。
- 后台尚无按用户设置有效期、角色、逐道菜权限或撤销已激活授权的界面。
- 现有激活流程核对的是平台与订单号，并不核验填写的邮箱或手机号是否为订单真实买家；请避免公开订单号。
